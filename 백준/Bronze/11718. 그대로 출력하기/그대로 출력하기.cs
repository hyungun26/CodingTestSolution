using System;

class Baek11718
{
    static void Main()
    {
        while (true)
        {
            string input = Console.ReadLine();
            if (string.IsNullOrEmpty(input) == true) break;
            Console.WriteLine(input);
        }
    }
}